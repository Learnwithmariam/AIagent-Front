# URL of the deployed API Worker, without a trailing slash.
# Baked in at build time. On Cloudflare Pages set it under Settings → Environment variables.
VITE_API_URL=https://gk-btu-students-api.<your-account>.workers.dev
